<!DOCTYPE html>
<html lang="ku" dir="rtl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ئازاد منهل - قورئانا پیرۆز</title>
    <link href="https://fonts.googleapis.com/css2?family=Amiri&family=Noto+Sans+Arabic:wght@400;700&display=swap" rel="stylesheet">
    <style>
        :root { --main-bg: #0f172a; --card-bg: #1e293b; --accent: #38bdf8; --text-color: #f1f5f9; --gold: #fbbf24; }
        body { font-family: 'Noto Sans Arabic', sans-serif; background-color: var(--main-bg); color: var(--text-color); margin: 0; padding-bottom: 80px; }
        
        /* Header */
        header { text-align: center; padding: 40px 20px; background: linear-gradient(to bottom, #1e293b, var(--main-bg)); border-bottom: 2px solid var(--accent); }
        .bismillah { font-family: 'Amiri', serif; font-size: 2.2rem; color: var(--gold); display: block; margin-bottom: 10px; }
        h1 { margin: 0; font-size: 1.8rem; color: var(--accent); }
        
        .container { max-width: 600px; margin: auto; padding: 15px; }
        
        /* Tabs */
        .tabs { display: flex; gap: 10px; margin-bottom: 20px; overflow-x: auto; padding-bottom: 10px; }
        .tab-btn { background: var(--card-bg); border: 1px solid var(--accent); color: white; padding: 10px 20px; border-radius: 20px; cursor: pointer; white-space: nowrap; }
        .tab-btn.active { background: var(--accent); color: var(--main-bg); font-weight: bold; }

        /* Content Sections */
        .section { display: none; }
        .section.active { display: block; }

        /* Card Style */
        .card { background: var(--card-bg); border-radius: 12px; padding: 15px; margin-bottom: 10px; display: flex; justify-content: space-between; align-items: center; border-right: 4px solid var(--accent); }
        .play-btn { background: var(--accent); border: none; width: 40px; height: 40px; border-radius: 50%; cursor: pointer; display: flex; align-items: center; justify-content: center; }

        /* About Section */
        .about-card { background: linear-gradient(45deg, #1e293b, #334155); padding: 25px; border-radius: 15px; text-align: center; border: 1px solid var(--accent); }
        .contact-btn { display: block; background: var(--gold); color: black; padding: 15px; border-radius: 10px; text-decoration: none; font-weight: bold; margin-top: 15px; }

        /* Audio Player Fixed */
        .player-footer { position: fixed; bottom: 0; width: 100%; background: #1e293b; border-top: 2px solid var(--accent); padding: 15px; display: none; text-align: center; }
        audio { width: 90%; height: 35px; }
    </style>
</head>
<body>

<header>
    <span class="bismillah">بِسْمِ اللَّهِ الرَّحْمَنِ الرَّحِيمِ</span>
    <h1>مالپەرێ ئازاد منهل</h1>
    <p style="color: #94a3b8;">ڕێکا رووناهیێ بەرەڤ بەختەوەریێ</p>
</header>

<div class="container">
    <div class="tabs">
        <button class="tab-btn active" onclick="showTab('quran')">📖 قورئان</button>
        <button class="tab-btn" onclick="showTab('names')">✨ ناڤێن خودێ</button>
        <button class="tab-btn" onclick="showTab('duaa')">🤲 دوعا</button>
        <button class="tab-btn" onclick="showTab('about')">👤 دەربارەی من</button>
    </div>

    <div id="quran" class="section active">
        <div id="surah-list"></div>
    </div>

    <div id="names" class="section">
        <div id="names-grid" style="display: grid; grid-template-columns: repeat(3, 1fr); gap: 10px;"></div>
    </div>

    <div id="duaa" class="section">
        <div class="card" style="display:block"><strong>دوعایا بەیانیان:</strong><br>اللهم بك أصبحنا وبك أمسينا...</div>
        <div class="card" style="display:block"><strong>دوعایا دایبابان:</strong><br>خودایێ من رەحمێ ب دایبابێن من ببەی...</div>
    </div>

    <div id="about" class="section">
        <div class="about-card">
            <h2 style="color: var(--gold);">ئازاد منهل</h2>
            <p>"خزمەتکرنا قورئانێ و مللەتی مەزنترین کارە."</p>
            <hr style="border: 0.5px solid #475569;">
            <p>بۆ هەر تێبینیەکێ پەیوەندیێ بکە:</p>
            <a href="tel:07506627505" class="contact-btn">📞 07506627505</a>
        </div>
    </div>
</div>

<div class="player-footer" id="audio-ui">
    <div id="now-playing" style="margin-bottom: 5px; font-size: 0.9rem; color: var(--accent);"></div>
    <audio id="main-player" controls></audio>
</div>

<script>
    // Tab System
    function showTab(id) {
        document.querySelectorAll('.section').forEach(s => s.classList.remove('active'));
        document.querySelectorAll('.tab-btn').forEach(b => b.classList.remove('active'));
        document.getElementById(id).classList.add('active');
        event.currentTarget.classList.add('active');
    }

    // Load Surahs (114)
    const surahList = document.getElementById('surah-list');
    const surahNames = ["الفاتحة", "البقرة", "آل عمران", "النساء", "المائدة", "الأنعام", "الأعراف", "الأنفال", "التوبة", "يونس"]; // نموونە
    
    for(let i=1; i<=114; i++) {
        let num = i.toString().padStart(3, '0');
        let card = document.createElement('div');
        card.className = 'card';
        card.innerHTML = `<span>${i}. سۆرەتا ${num}</span> 
            <button class="play-btn" onclick="playAudio('https://server6.mp3quran.net/thubti/${num}.mp3', 'سۆرەتا ${i}')">▶️</button>`;
        surahList.appendChild(card);
    }

    // Load Names
    const names = ["الرحمن", "الرحيم", "الملك", "القدوس", "السلام", "المؤمن", "المهيمن", "العزيز"];
    const namesGrid = document.getElementById('names-grid');
    names.forEach(n => {
        let d = document.createElement('div');
        d.className = 'card'; d.style.justifyContent = 'center'; d.innerText = n;
        namesGrid.appendChild(d);
    });

    // Audio Player
    function playAudio(url, title) {
        const ui = document.getElementById('audio-ui');
        const player = document.getElementById('main-player');
        document.getElementById('now-playing').innerText = "نوکە لێددەت: " + title;
        ui.style.display = 'block';
        player.src = url;
        player.play();
    }
</script>

</body>
</html>
